{{JSON.stringify({
  jobs: (() => {
    const job = this.params?.job || appsmith.store.selectedMailoutJob;
    return job ? [job] : [];
  })(),
  delivery_mode: this.params?.delivery_mode || SelectDeliveryMode.selectedOptionValue || 'test'
})}}
