INSERT INTO public.email_templates (
  template_key,
  template_name,
  template_type,
  subject_template,
  text_template,
  html_template,
  is_active,
  created_at,
  updated_at
)
VALUES (
  {{InputTemplateKey.text || ''}},
  {{InputTemplateName.text || ''}},
  {{(() => { const v = SelectTemplateType.selectedOptionValue; if (v === 'Email') return 0; if (v === 'Header') return 1; if (v === 'Signature') return 2; const n = Number(v); return Number.isFinite(n) ? n : 0; })()}},
  {{InputSubject.text || null}},
  {{TextareaTextTemplate.text || null}},
  {{TextareaHtmlTemplate.text || null}},
  {{CheckboxIsActive.isSwitchedOn}},
  now(),
  now()
)
RETURNING id;