UPDATE public.teams SET section = '{{EditInputSection.text}}', team_name = '{{EditInputTeamName.text}}', season = '{{EditInputSeason.text}}', sort_order = {{EditInputSortOrder.text || 0}}, updated_at = now() WHERE id = {{(appsmith.store.teamManagementSelectedId || TableTeams.selectedRow.id)}} RETURNING id;